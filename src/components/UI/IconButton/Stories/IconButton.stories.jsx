import IconButton from "../IconButton"

export default {
    title: "IconButton",
    component: IconButton,
    tags: ["autodocs"],
}

export const small = {
    args: {
        size: "small",
    }
}

export const medium = {
    args: {
        size: "medium",
    }
}

export const large = {
    args: {
        size: "large",
    }
}