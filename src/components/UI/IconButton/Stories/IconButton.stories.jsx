import IconButton from "../IconButton";

export default {
    title: 'UI / IconButton',
    component: IconButton,
    tags: ['autodocs'],
};


export const Primary = {
    args: {
        size: "medium",
    }
}

export const Secondary = {
    args: {
        size: "large"
    }
}