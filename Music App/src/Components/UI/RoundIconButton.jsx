import React, { Fragment } from "react";

const RoundIconButton = (props) => {
    const {type} = props;

    return (
        <Fragment>
            <button> </button>
        </Fragment>
    )

}

export default RoundIconButton;